# Divider — `hr`

Tag: `hr`

## JSON Example

```json
{ "tag": "hr", "margin": "0px" }
```

## Key Fields

- **tag**: `"hr"` (required)
- **element_id**: Optional unique identifier
- **margin**: Component margin

Simple horizontal line divider. No other configurable fields.
